现在进度
已完成采集器-》采集器待优化：需要协程化


待优化：
增量爬取机制，使用读取读取最后时间，采集器要对应数据库格式
现在目标：三级过滤中：ac自动机关键词过滤-》向量化搜索。


船舶数字化转型信息系统数据采集业务逻辑：
背景：一个模型对应一个数据源，每个模型都绑定了多个关键词，对于每个关键词都有一个时间戳水位线。


层次任务定义：
fetcher：按照模型最低水位线爬取所有可能索引。
process：
        Filter (初筛)：利用关键词进行初步的时间与内容布尔过滤（或者ac自动机）。
        Vector Engine（二次筛选）：如果不存在相关性就舍弃。
        Labeling (打标)：给出可能方向
        dedup_engine：url指纹，语义去重
        clear：去除噪声和格式
        extractor：deepseek结构化提取
        geo_encoder：最后附加地理信息

任务驱动：
1. 启动模型->查询模型活跃关键词->最低水位线
|

2. 进入索引层
index-fetcher（大量索引）

3. 加工层
3.1 关键词粒度筛选